embedding:
  provider: sentence_transformers
  model:    sentence-transformers/all-mpnet-base-v2
  parameters:
    batch_size: 32

chat:
  provider: openai
  model:    gpt-4o-mini
  parameters:
    temperature: 0.2
    max_tokens:  512
    top_p:       1.0
